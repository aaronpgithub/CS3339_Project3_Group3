package main

func (c *Control) issueProcess(bufPreIssue *Queue) {
	/*for _, i := range bufPreIssue.data {

	}

	if err != -1 { //if buffer isn't empty
		//is memBuffer has space
		//is aluBuffer has space

		var instructionType = value
	}

	//c.runInstruction(value[0].(Instruction))*/
}
